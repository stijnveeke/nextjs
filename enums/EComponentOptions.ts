export const enum EComponentOptions {
  noImage = "noImage",
  noContent = "noContent",
  noTitle = "noTitle",
  horizontal = "horizontal",
  noPrice = "noPrice",
  noSubHeaders = "noSubHeaders",
  useReadMore = "useReadMore",
  noContainer = "noContainer",
}

export default EComponentOptions;
